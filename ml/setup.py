from setuptools import setup

setup(
   name='ml',
   version='1.0.0',
   description='A useful module',
   install_requires=[],
   author='Ilnaz',
   author_email='chiezescrap@mail.ru',
   packages=['src', 'models']
)