# Example environment variables for local development
POSTGRES_DB=plant_db
POSTGRES_USER=plant_user
POSTGRES_PASSWORD=plant_password
POSTGRES_HOST=db
POSTGRES_PORT=5432
# The migrator service uses the variables above to connect and manage the schema.
# For local development outside Docker, override POSTGRES_HOST=localhost in your personal .env file.
# DATABASE_URL can be used instead of the individual parameters above.
# DATABASE_URL=postgresql+psycopg2://plant_user:plant_password@localhost:5432/plant_db
